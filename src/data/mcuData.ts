import { MCUItem } from '../types';

// Deprecated static dataset. All catalog items are now dynamically loaded directly from /public/mcu_catalog.csv
export const mcuCatalog: MCUItem[] = [];
export const phaseList: string[] = ['All'];
